from bayesClassifier import naiveBayes;
class Supervisedclassifier:
    name
    def __init__(self, name):
        self.name=name
    
    def __init__(self):
        self.name="Naive Bayes"

# the training method returns the joint distribution of the 
    def training(data):
        if name=="naiveBayes":
            return bayesClassifier.naiveBayes(data) # returns the mean vetor for the classes and variance vect correspond to each class that is these all for all the classes

        elif name=="bayes":
            return bayesClassifier.bayes(data)    #actually returs a vector of the covariance matrix and the mean vector corresponding to the each class output 
        
            
